<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HR Payroll Management System</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary-dark: #1a472a;
            --primary: #2d6a4f;
            --primary-light: #40916c;
            --accent: #52b788;
            --success: #2d6a4f;
            --danger: #dc2f02;
            --warning: #e85d04;
            --info: #1b4332;
            --background: #ffffff;
            --surface: #ffffff;
            --border: #e9ecef;
            --text-primary: #212529;
            --text-secondary: #495057;
            --text-muted: #6c757d;
            --shadow: 0 1px 3px rgba(0, 0, 0, 0.05);
            --shadow-lg: 0 10px 25px rgba(0, 0, 0, 0.05);
            --radius-sm: 0.375rem;
            --radius-md: 0.5rem;
            --radius-lg: 0.75rem;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', sans-serif;
            background-color: var(--background);
            color: var(--text-primary);
            line-height: 1.5;
            height: 100vh;
            overflow: hidden;
        }

        .app-layout {
            display: flex;
            height: 100vh;
            width: 100%;
        }

        /* Sidebar */
        .sidebar {
            width: 280px;
            background: var(--surface);
            border-right: 1px solid var(--border);
            display: flex;
            flex-direction: column;
            position: fixed;
            height: 100vh;
            left: 0;
            top: 0;
            z-index: 10;
            box-shadow: var(--shadow);
        }

        .sidebar-header {
            padding: 1.75rem 1.5rem;
            border-bottom: 1px solid var(--border);
            margin-bottom: 1.5rem;
        }

        .sidebar-header h1 {
            font-size: 1.25rem;
            font-weight: 700;
            color: var(--primary);
        }

        .sidebar-header p {
            font-size: 0.75rem;
            color: var(--text-muted);
            margin-top: 0.5rem;
        }

        .nav-menu {
            flex: 1;
            padding: 0 1rem;
        }

        .nav-item {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            margin-bottom: 0.5rem;
            border-radius: var(--radius-md);
            color: var(--text-secondary);
            background: transparent;
            border: none;
            width: 100%;
            cursor: pointer;
            font-size: 0.875rem;
            font-weight: 500;
            transition: all 0.2s ease;
            text-align: left;
            font-family: inherit;
        }

        .nav-item svg {
            width: 1.25rem;
            height: 1.25rem;
            stroke: currentColor;
            fill: none;
            stroke-width: 2;
        }

        .nav-item:hover {
            background-color: #f8f9fa;
            color: var(--primary);
        }

        .nav-item.active {
            background-color: rgba(45, 106, 79, 0.08);
            color: var(--primary);
            border-left: 3px solid var(--primary);
        }

        .sidebar-footer {
            padding: 1.5rem;
            border-top: 1px solid var(--border);
        }

        .logout-btn {
            display: flex;
            align-items: center;
            justify-content: flex-start;
            padding: 0.75rem 0;
            background: transparent;
            border: none;
            color: var(--text-secondary);
            font-size: 0.875rem;
            font-weight: 500;
            cursor: pointer;
            transition: all 0.2s ease;
            font-family: inherit;
            width: 100%;
            text-align: left;
        }

        .logout-btn:hover {
            color: var(--danger);
        }

        /* Main Content */
        .main-content {
            flex: 1;
            margin-left: 280px;
            overflow-y: auto;
            padding: 2rem;
            background-color: #f8f9fa;
        }

        /* Auth Container */
        .auth-container {
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
        }

        .auth-card {
            background: var(--surface);
            padding: 2.5rem;
            border-radius: var(--radius-lg);
            box-shadow: 0 20px 35px rgba(0, 0, 0, 0.1);
            text-align: center;
            max-width: 420px;
            width: 90%;
            border: 1px solid var(--border);
        }

        .auth-card h1 {
            font-size: 1.5rem;
            margin-bottom: 0.5rem;
            color: var(--primary);
        }

        .auth-card p {
            color: var(--text-secondary);
            margin-bottom: 1.5rem;
            font-size: 0.875rem;
        }

        /* Cards */
        .card {
            background: var(--surface);
            border-radius: var(--radius-lg);
            border: 1px solid var(--border);
            margin-bottom: 1.5rem;
            overflow: hidden;
            box-shadow: var(--shadow);
        }

        .card-header {
            padding: 1.25rem 1.5rem;
            border-bottom: 1px solid var(--border);
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 1rem;
            background: #fafbfc;
        }

        .card-header h2 {
            font-size: 1.125rem;
            font-weight: 600;
            color: var(--text-primary);
        }

        .card-body {
            padding: 1.5rem;
        }

        /* Forms */
        .form-group {
            margin-bottom: 1.25rem;
        }

        .form-group label {
            display: block;
            margin-bottom: 0.5rem;
            font-size: 0.8125rem;
            font-weight: 500;
            color: var(--text-secondary);
        }

        .form-group input,
        .form-group select {
            width: 100%;
            padding: 0.625rem 0.875rem;
            background-color: var(--surface);
            border: 1px solid var(--border);
            border-radius: var(--radius-sm);
            color: var(--text-primary);
            font-size: 0.875rem;
            transition: all 0.2s ease;
            font-family: inherit;
        }

        .form-group input:focus,
        .form-group select:focus {
            outline: none;
            border-color: var(--primary);
            box-shadow: 0 0 0 3px rgba(45, 106, 79, 0.1);
        }

        .form-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1rem;
        }

        .form-row-3 {
            display: grid;
            grid-template-columns: 1fr 1fr 1fr;
            gap: 1rem;
        }

        /* Buttons */
        .btn {
            padding: 0.5rem 1rem;
            border-radius: var(--radius-sm);
            font-size: 0.8125rem;
            font-weight: 500;
            cursor: pointer;
            transition: all 0.2s ease;
            border: none;
            font-family: inherit;
        }

        .btn-primary {
            background-color: var(--primary);
            color: white;
        }

        .btn-primary:hover {
            background-color: var(--primary-dark);
        }

        .btn-success {
            background-color: #e8f5e9;
            color: var(--success);
            border: 1px solid #c8e6c9;
        }

        .btn-success:hover {
            background-color: #c8e6c9;
        }

        .btn-danger {
            background-color: #ffe5e5;
            color: var(--danger);
            border: 1px solid #ffcdd2;
        }

        .btn-danger:hover {
            background-color: #ffcdd2;
        }

        .btn-secondary {
            background-color: #e9ecef;
            color: var(--text-secondary);
        }

        .btn-secondary:hover {
            background-color: #dee2e6;
        }

        /* Tables */
        .table-wrapper {
            overflow-x: auto;
            max-height: 500px;
            overflow-y: auto;
        }

        .data-table {
            width: 100%;
            border-collapse: collapse;
            font-size: 0.75rem;
        }

        .data-table thead tr {
            border-bottom: 1px solid var(--border);
            position: sticky;
            top: 0;
            background: #fafbfc;
            z-index: 1;
        }

        .data-table th {
            text-align: left;
            padding: 0.75rem 0.5rem;
            font-weight: 600;
            color: var(--text-muted);
            background-color: #fafbfc;
            white-space: nowrap;
        }

        .data-table td {
            padding: 0.75rem 0.5rem;
            color: var(--text-secondary);
            border-bottom: 1px solid var(--border);
            white-space: nowrap;
        }

        .data-table tbody tr:hover {
            background-color: #f8f9fa;
        }

        .totals-row {
            background-color: #e8f5e9;
            font-weight: 600;
            border-top: 2px solid var(--primary);
        }

        .totals-row td {
            color: var(--primary-dark);
            font-weight: 600;
        }

        /* Stats */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1rem;
            margin-bottom: 1.5rem;
        }

        .stat-card {
            background: var(--surface);
            border: 1px solid var(--border);
            border-radius: var(--radius-lg);
            padding: 1rem;
        }

        .stat-card h3 {
            font-size: 0.7rem;
            font-weight: 600;
            text-transform: uppercase;
            color: var(--text-muted);
            margin-bottom: 0.25rem;
        }

        .stat-card .value {
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--primary);
        }

        /* Alerts */
        .alert {
            padding: 0.75rem 1rem;
            border-radius: var(--radius-md);
            margin-bottom: 1rem;
            border-left: 3px solid;
            font-size: 0.875rem;
        }

        .alert-success {
            background-color: #e8f5e9;
            border-left-color: var(--success);
            color: var(--success);
        }

        .alert-error {
            background-color: #ffe5e5;
            border-left-color: var(--danger);
            color: var(--danger);
        }

        .tab-content {
            display: none;
        }

        .tab-content.active {
            display: block;
        }

        .hidden {
            display: none !important;
        }

        .action-buttons {
            display: flex;
            gap: 0.5rem;
            flex-wrap: wrap;
        }

        @media (max-width: 768px) {
            .sidebar {
                width: 100%;
                height: auto;
                position: relative;
            }
            .main-content {
                margin-left: 0;
            }
            .app-layout {
                flex-direction: column;
            }
            .form-row, .form-row-3 {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <div id="authContainer" class="auth-container">
        <div class="auth-card">
            <h1>HR Payroll Management</h1>
            <p>Connect with Google Sheets to manage employee payroll</p>
            <button id="loginBtn" class="btn btn-primary" style="width: 100%; padding: 0.75rem;">Sign in with Google</button>
            <div id="errorMessage" style="margin-top: 1rem; font-size: 0.75rem; color: var(--danger);"></div>
        </div>
    </div>

    <div id="appContainer" class="hidden">
        <div class="app-layout">
            <aside class="sidebar">
                <div class="sidebar-header">
                    <h1>HR Payroll System</h1>
                    <p>Employee & Payroll Management</p>
                </div>
                <nav class="nav-menu">
                    <button class="nav-item active" data-tab="dashboard">
                        <svg viewBox="0 0 24 24"><path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2h-5v-7H9v7H4a2 2 0 0 1-2-2z"/></svg>
                        <span>Dashboard</span>
                    </button>
                    <button class="nav-item" data-tab="payroll">
                        <svg viewBox="0 0 24 24"><path d="M12 2v4M12 18v4M4.93 4.93l2.83 2.83M16.24 16.24l2.83 2.83M2 12h4M18 12h4M4.93 19.07l2.83-2.83M16.24 7.76l2.83-2.83"/><circle cx="12" cy="12" r="3"/></svg>
                        <span>Payroll</span>
                    </button>
                    <button class="nav-item" data-tab="employees">
                        <svg viewBox="0 0 24 24"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>
                        <span>Employees</span>
                    </button>
                    <button class="nav-item" data-tab="logs">
                        <svg viewBox="0 0 24 24"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></svg>
                        <span>Activity Logs</span>
                    </button>
                </nav>
                <div class="sidebar-footer">
                    <button id="logoutBtn" class="logout-btn">Logout</button>
                </div>
            </aside>

            <main class="main-content">
                <div id="alertContainer"></div>

                <!-- Dashboard Tab -->
                <div id="dashboard" class="tab-content active">
                    <div class="stats-grid">
                        <div class="stat-card"><h3>Total Employees</h3><div class="value" id="totalEmployees">0</div></div>
                        <div class="stat-card"><h3>Total Gross Pay</h3><div class="value" id="totalGross">ETB 0</div></div>
                        <div class="stat-card"><h3>Total Tax</h3><div class="value" id="totalTax">ETB 0</div></div>
                        <div class="stat-card"><h3>Total Net Pay</h3><div class="value" id="totalNet">ETB 0</div></div>
                    </div>
                    <div class="card">
                        <div class="card-header">
                            <h2>Payroll Summary</h2>
                            <button onclick="exportToExcel()" class="btn btn-primary">Export to Excel</button>
                        </div>
                        <div class="card-body">
                            <div class="table-wrapper" id="payrollSummaryContainer"></div>
                        </div>
                    </div>
                </div>

                <!-- Payroll Tab -->
                <div id="payroll" class="tab-content">
                    <div class="card">
                        <div class="card-header"><h2>➕ Add/Edit Payroll Record</h2></div>
                        <div class="card-body">
                            <form id="payrollForm">
                                <input type="hidden" id="recordId">
                                <div class="form-row">
                                    <div class="form-group"><label>Employee Name *</label><input type="text" id="empName" required placeholder="Full name"></div>
                                    <div class="form-group"><label>Basic Salary *</label><input type="number" id="basicSalary" required step="0.01" min="0" placeholder="0.00"></div>
                                </div>
                                <div class="form-row">
                                    <div class="form-group"><label>Transport Allowance</label><input type="number" id="transportAllowance" step="0.01" min="0" value="0"></div>
                                    <div class="form-group"><label>Taxable Transport Allowance</label><input type="number" id="taxableTransport" step="0.01" min="0" value="0"></div>
                                </div>
                                <div class="form-row">
                                    <div class="form-group"><label>Overtime Pay</label><input type="number" id="overtimePay" step="0.01" min="0" value="0"></div>
                                    <div class="form-group"><label>Loan Deduction</label><input type="number" id="loanDeduction" step="0.01" min="0" value="0"></div>
                                </div>
                                <div class="action-buttons">
                                    <button type="submit" class="btn btn-primary">Save Record</button>
                                    <button type="button" onclick="clearForm()" class="btn btn-secondary">Clear</button>
                                </div>
                            </form>
                        </div>
                    </div>
                    
                    <div class="card">
                        <div class="card-header"><h2>📋 Payroll Register</h2></div>
                        <div class="card-body">
                            <div class="table-wrapper" id="payrollTableContainer"></div>
                        </div>
                    </div>
                </div>

                <!-- Employees Tab -->
                <div id="employees" class="tab-content">
                    <div class="card">
                        <div class="card-header"><h2>👥 Employee Directory</h2></div>
                        <div class="card-body">
                            <div class="table-wrapper" id="employeeListContainer"></div>
                        </div>
                    </div>
                </div>

                <!-- Logs Tab -->
                <div id="logs" class="tab-content">
                    <div class="card">
                        <div class="card-header"><h2>📜 Activity Logs</h2></div>
                        <div class="card-body">
                            <div class="table-wrapper" id="activityLogsContainer"></div>
                        </div>
                    </div>
                </div>
            </main>
        </div>
    </div>

    <script src="https://apis.google.com/js/api.js"></script>
    <script src="https://accounts.google.com/gsi/client"></script>
    <script src="https://cdn.jsdelivr.net/npm/xlsx@0.18.5/dist/xlsx.full.min.js"></script>

    <script>
        // Configuration
        const CONFIG = {
            CLIENT_ID: '1058574831034-cb30fi18f3f05937969jksvhipso5hav.apps.googleusercontent.com',
            API_KEY: 'AIzaSyD4oMaBJeXQy8JCpzqcOAJ9HexNAKCPlyY',
            SPREADSHEET_ID: '1NnOJFFLbPtuPYbI53dXIKA_KezdnxhCiqsPteuvL1Fc',
            SCOPES: 'https://www.googleapis.com/auth/spreadsheets'
        };

        let tokenClient = null;
        let isAuthenticated = false;
        let payrollRecords = [];
        let activityLogs = [];

        // Ethiopian Tax Calculation
        function calculateEthiopianTax(taxableIncome) {
            if (taxableIncome <= 2000) return 0;
            if (taxableIncome <= 4000) return (taxableIncome * 0.15) - 300;
            if (taxableIncome <= 7000) return (taxableIncome * 0.20) - 500;
            if (taxableIncome <= 10000) return (taxableIncome * 0.25) - 850;
            if (taxableIncome <= 14000) return (taxableIncome * 0.30) - 1350;
            return (taxableIncome * 0.35) - 2050;
        }

        // Calculate all payroll fields
        function calculatePayroll(record) {
            const salary = parseFloat(record.basicSalary) || 0;
            const transport = parseFloat(record.transportAllowance) || 0;
            const overtime = parseFloat(record.overtimePay) || 0;
            const taxableTransport = parseFloat(record.taxableTransport) || 0;
            const loan = parseFloat(record.loanDeduction) || 0;

            const grossPay = salary + transport + overtime;
            const taxableIncome = salary + (taxableTransport > 0 ? taxableTransport : overtime);
            const incomeTax = calculateEthiopianTax(taxableIncome);
            const pension7 = salary * 0.07;
            const pension11 = salary * 0.11;
            const totalDeductions = incomeTax + pension7 + loan;
            const netPay = grossPay - totalDeductions;

            return {
                grossPay, taxableIncome, incomeTax, pension7, pension11,
                totalDeductions, netPay
            };
        }

        // Update full record with calculations
        function updateRecordWithCalculations(record) {
            const calc = calculatePayroll(record);
            return {
                ...record,
                grossPay: calc.grossPay,
                taxableIncome: calc.taxableIncome,
                incomeTax: calc.incomeTax,
                pension7: calc.pension7,
                pension11: calc.pension11,
                totalDeductions: calc.totalDeductions,
                netPay: calc.netPay
            };
        }

        // Tab Navigation
        function switchToTab(tabName) {
            document.querySelectorAll('.tab-content').forEach(tab => tab.classList.remove('active'));
            document.querySelectorAll('.nav-item').forEach(item => item.classList.remove('active'));
            document.getElementById(tabName).classList.add('active');
            document.querySelector(`.nav-item[data-tab="${tabName}"]`).classList.add('active');
        }

        function initTabListeners() {
            document.querySelectorAll('.nav-item').forEach(btn => {
                btn.removeEventListener('click', btn._listener);
                btn._listener = () => switchToTab(btn.getAttribute('data-tab'));
                btn.addEventListener('click', btn._listener);
            });
        }

        window.switchToTab = switchToTab;

        // Show alert
        function showAlert(message, type) {
            const container = document.getElementById('alertContainer');
            const id = 'alert-' + Date.now();
            const div = document.createElement('div');
            div.id = id;
            div.className = `alert alert-${type}`;
            div.textContent = message;
            container.appendChild(div);
            setTimeout(() => document.getElementById(id)?.remove(), 5000);
        }

        // Validate inputs
        function validateRecord(record) {
            if (!record.employeeName || record.employeeName.trim() === '') {
                showAlert('Employee name is required', 'error');
                return false;
            }
            if (record.basicSalary < 0) {
                showAlert('Basic salary cannot be negative', 'error');
                return false;
            }
            return true;
        }

        // Load data from Google Sheet
        async function loadAllData() {
            if (!isAuthenticated) return;
            try {
                const payrollResp = await gapi.client.sheets.spreadsheets.values.get({
                    spreadsheetId: CONFIG.SPREADSHEET_ID,
                    range: 'Payroll!A:O'
                });
                const values = payrollResp.result.values || [];
                if (values.length > 1) {
                    payrollRecords = values.slice(1).map(row => ({
                        id: row[0],
                        employeeName: row[1],
                        basicSalary: parseFloat(row[2]) || 0,
                        transportAllowance: parseFloat(row[3]) || 0,
                        taxableTransport: parseFloat(row[4]) || 0,
                        overtimePay: parseFloat(row[5]) || 0,
                        loanDeduction: parseFloat(row[6]) || 0,
                        grossPay: parseFloat(row[7]) || 0,
                        taxableIncome: parseFloat(row[8]) || 0,
                        incomeTax: parseFloat(row[9]) || 0,
                        pension7: parseFloat(row[10]) || 0,
                        pension11: parseFloat(row[11]) || 0,
                        totalDeductions: parseFloat(row[12]) || 0,
                        netPay: parseFloat(row[13]) || 0,
                        signature: row[14] || ''
                    }));
                } else {
                    payrollRecords = [];
                }

                const logsResp = await gapi.client.sheets.spreadsheets.values.get({
                    spreadsheetId: CONFIG.SPREADSHEET_ID,
                    range: 'ActivityLogs!A:D'
                });
                const logValues = logsResp.result.values || [];
                activityLogs = logValues.slice(1).map(row => ({
                    timestamp: row[0], action: row[1], details: row[2], user: row[3]
                }));

                updateUI();
            } catch (error) {
                console.error(error);
                showAlert('Error loading data: ' + error.message, 'error');
            }
        }

        // Save to Google Sheet
        async function saveAllData() {
            if (!isAuthenticated) return;
            try {
                const payrollData = [['ID', 'EmployeeName', 'BasicSalary', 'TransportAllowance', 'TaxableTransport', 'OvertimePay', 'LoanDeduction', 'GrossPay', 'TaxableIncome', 'IncomeTax', 'Pension7', 'Pension11', 'TotalDeductions', 'NetPay', 'Signature']];
                payrollRecords.forEach(rec => {
                    payrollData.push([
                        rec.id, rec.employeeName, rec.basicSalary, rec.transportAllowance,
                        rec.taxableTransport, rec.overtimePay, rec.loanDeduction,
                        rec.grossPay, rec.taxableIncome, rec.incomeTax, rec.pension7,
                        rec.pension11, rec.totalDeductions, rec.netPay, rec.signature || ''
                    ]);
                });
                await gapi.client.sheets.spreadsheets.values.clear({
                    spreadsheetId: CONFIG.SPREADSHEET_ID,
                    range: 'Payroll!A:O'
                });
                await gapi.client.sheets.spreadsheets.values.update({
                    spreadsheetId: CONFIG.SPREADSHEET_ID,
                    range: 'Payroll!A1',
                    valueInputOption: 'RAW',
                    resource: { values: payrollData }
                });
            } catch (error) {
                console.error('Error saving payroll:', error);
            }
        }

        async function logActivity(action, details) {
            try {
                await gapi.client.sheets.spreadsheets.values.append({
                    spreadsheetId: CONFIG.SPREADSHEET_ID,
                    range: 'ActivityLogs!A:D',
                    valueInputOption: 'RAW',
                    resource: { values: [[new Date().toISOString(), action, details, 'Manager']] }
                });
            } catch (error) { console.error(error); }
            await loadAllData();
        }

        // Add/Update record
        window.saveRecord = async function(record) {
            if (!validateRecord(record)) return false;
            const calculated = updateRecordWithCalculations(record);
            
            if (calculated.id) {
                const index = payrollRecords.findIndex(r => r.id === calculated.id);
                if (index !== -1) {
                    payrollRecords[index] = calculated;
                    await logActivity('Updated payroll record', `${calculated.employeeName}`);
                }
            } else {
                calculated.id = 'EMP-' + Date.now();
                payrollRecords.push(calculated);
                await logActivity('Added payroll record', `${calculated.employeeName}`);
            }
            await saveAllData();
            await loadAllData();
            clearForm();
            return true;
        };

        window.deleteRecord = async function(id) {
            if (confirm('Are you sure you want to delete this record?')) {
                const record = payrollRecords.find(r => r.id === id);
                payrollRecords = payrollRecords.filter(r => r.id !== id);
                await saveAllData();
                await logActivity('Deleted payroll record', record?.employeeName || 'Unknown');
                await loadAllData();
                showAlert('Record deleted successfully', 'success');
            }
        };

        window.editRecord = function(id) {
            const record = payrollRecords.find(r => r.id === id);
            if (record) {
                document.getElementById('recordId').value = record.id;
                document.getElementById('empName').value = record.employeeName;
                document.getElementById('basicSalary').value = record.basicSalary;
                document.getElementById('transportAllowance').value = record.transportAllowance;
                document.getElementById('taxableTransport').value = record.taxableTransport;
                document.getElementById('overtimePay').value = record.overtimePay;
                document.getElementById('loanDeduction').value = record.loanDeduction;
                switchToTab('payroll');
            }
        };

        function clearForm() {
            document.getElementById('recordId').value = '';
            document.getElementById('empName').value = '';
            document.getElementById('basicSalary').value = '';
            document.getElementById('transportAllowance').value = '0';
            document.getElementById('taxableTransport').value = '0';
            document.getElementById('overtimePay').value = '0';
            document.getElementById('loanDeduction').value = '0';
        }

        // Export to Excel
        window.exportToExcel = function() {
            const exportData = [['ተ.ቁ', 'ሰም', 'ደመወዝ', 'የትራንስፖርት አበል', 'ታክሴብል የትራንስፖርት አበል', 'ትርፍ ሰዓት', 'የካዝና መጠባበቂያ', 'ጠቅላላ ተከፋይ', 'ግብር የሚከፈልበት', 'ተቀናሽ የሥራ ግብር', 'የጡረታ ተቀናሽ 7%', 'የጡረታ ተቀናሽ 11%', 'ብድር', 'ጠቅላላ ተቀናሽ', 'የተጣራ ክፍያ', 'ፊርማ']];
            
            payrollRecords.forEach((rec, idx) => {
                exportData.push([
                    idx + 1,
                    rec.employeeName,
                    rec.basicSalary,
                    rec.transportAllowance,
                    rec.taxableTransport,
                    rec.overtimePay,
                    rec.loanDeduction,
                    rec.grossPay,
                    rec.taxableIncome,
                    rec.incomeTax,
                    rec.pension7,
                    rec.pension11,
                    rec.loanDeduction,
                    rec.totalDeductions,
                    rec.netPay,
                    rec.signature || ''
                ]);
            });
            
            const ws = XLSX.utils.aoa_to_sheet(exportData);
            const wb = XLSX.utils.book_new();
            XLSX.utils.book_append_sheet(wb, ws, 'Payroll');
            XLSX.writeFile(wb, `Payroll_${new Date().toISOString().split('T')[0]}.xlsx`);
            showAlert('Exported to Excel successfully', 'success');
        };

        // Update UI
        function updateUI() {
            const container = document.getElementById('payrollTableContainer');
            const summaryContainer = document.getElementById('payrollSummaryContainer');
            const employeeContainer = document.getElementById('employeeListContainer');
            
            let totals = {
                basicSalary: 0, transportAllowance: 0, taxableTransport: 0,
                overtimePay: 0, loanDeduction: 0, grossPay: 0,
                taxableIncome: 0, incomeTax: 0, pension7: 0,
                pension11: 0, totalDeductions: 0, netPay: 0
            };

            // Payroll Table
            if (payrollRecords.length === 0) {
                container.innerHTML = '<div class="text-secondary" style="padding: 2rem; text-align: center;">No payroll records found</div>';
                summaryContainer.innerHTML = '<div class="text-secondary" style="padding: 2rem; text-align: center;">No data available</div>';
            } else {
                let html = `<table><thead><tr>
                    <th>ተ.ቁ</th><th>ሰም</th><th>ደመወዝ</th><th>የትራንስፖርት አበል</th><th>ታክሴብል የትራንስፖርት አበል</th>
                    <th>ትርፍ ሰዓት</th><th>የካዝና መጠባበቂያ</th><th>ጠቅላላ ተከፋይ</th><th>ግብር የሚከፈልበት</th>
                    <th>ተቀናሽ የሥራ ግብር</th><th>የጡረታ ተቀናሽ 7%</th><th>የጡረታ ተቀናሽ 11%</th>
                    <th>ብድር</th><th>ጠቅላላ ተቀናሽ</th><th>የተጣራ ክፍያ</th><th>ፊርማ</th><th>Actions</th>
                </tr></thead><tbody>`;
                
                payrollRecords.forEach((rec, idx) => {
                    totals.basicSalary += rec.basicSalary;
                    totals.transportAllowance += rec.transportAllowance;
                    totals.taxableTransport += rec.taxableTransport;
                    totals.overtimePay += rec.overtimePay;
                    totals.loanDeduction += rec.loanDeduction;
                    totals.grossPay += rec.grossPay;
                    totals.taxableIncome += rec.taxableIncome;
                    totals.incomeTax += rec.incomeTax;
                    totals.pension7 += rec.pension7;
                    totals.pension11 += rec.pension11;
                    totals.totalDeductions += rec.totalDeductions;
                    totals.netPay += rec.netPay;
                    
                    html += `<tr>
                        <td>${idx + 1}</td>
                        <td><strong>${rec.employeeName}</strong></td>
                        <td>${rec.basicSalary.toFixed(2)}</td>
                        <td>${rec.transportAllowance.toFixed(2)}</td>
                        <td>${rec.taxableTransport.toFixed(2)}</td>
                        <td>${rec.overtimePay.toFixed(2)}</td>
                        <td>${rec.loanDeduction.toFixed(2)}</td>
                        <td>${rec.grossPay.toFixed(2)}</td>
                        <td>${rec.taxableIncome.toFixed(2)}</td>
                        <td>${rec.incomeTax.toFixed(2)}</td>
                        <td>${rec.pension7.toFixed(2)}</td>
                        <td>${rec.pension11.toFixed(2)}</td>
                        <td>${rec.loanDeduction.toFixed(2)}</td>
                        <td>${rec.totalDeductions.toFixed(2)}</td>
                        <td><strong>${rec.netPay.toFixed(2)}</strong></td>
                        <td>${rec.signature || ''}</td>
                        <td class="action-buttons">
                            <button class="btn btn-success" onclick="editRecord('${rec.id}')">Edit</button>
                            <button class="btn btn-danger" onclick="deleteRecord('${rec.id}')">Delete</button>
                        </td>
                    </tr>`;
                });
                
                html += `<tr class="totals-row">
                    <td colspan="2"><strong>Totals</strong></td>
                    <td><strong>${totals.basicSalary.toFixed(2)}</strong></td>
                    <td><strong>${totals.transportAllowance.toFixed(2)}</strong></td>
                    <td><strong>${totals.taxableTransport.toFixed(2)}</strong></td>
                    <td><strong>${totals.overtimePay.toFixed(2)}</strong></td>
                    <td><strong>${totals.loanDeduction.toFixed(2)}</strong></td>
                    <td><strong>${totals.grossPay.toFixed(2)}</strong></td>
                    <td><strong>${totals.taxableIncome.toFixed(2)}</strong></td>
                    <td><strong>${totals.incomeTax.toFixed(2)}</strong></td>
                    <td><strong>${totals.pension7.toFixed(2)}</strong></td>
                    <td><strong>${totals.pension11.toFixed(2)}</strong></td>
                    <td><strong>${totals.loanDeduction.toFixed(2)}</strong></td>
                    <td><strong>${totals.totalDeductions.toFixed(2)}</strong></td>
                    <td><strong>${totals.netPay.toFixed(2)}</strong></td>
                    <td></td><td></td>
                </tr>`;
                html += '</tbody></table>';
                container.innerHTML = html;

                // Summary table for dashboard
                let summaryHtml = '<table class="data-table"><thead><tr><th>Employee</th><th>Gross Pay</th><th>Income Tax</th><th>Pension 7%</th><th>Loan</th><th>Net Pay</th></tr></thead><tbody>';
                payrollRecords.forEach(rec => {
                    summaryHtml += `<tr>
                        <td>${rec.employeeName}</td>
                        <td>${rec.grossPay.toFixed(2)}</td>
                        <td>${rec.incomeTax.toFixed(2)}</td>
                        <td>${rec.pension7.toFixed(2)}</td>
                        <td>${rec.loanDeduction.toFixed(2)}</td>
                        <td><strong>${rec.netPay.toFixed(2)}</strong></td>
                    </tr>`;
                });
                summaryHtml += `</tbody></table>`;
                summaryContainer.innerHTML = summaryHtml;
            }

            // Employee list
            if (payrollRecords.length === 0) {
                employeeContainer.innerHTML = '<div class="text-secondary" style="padding: 2rem; text-align: center;">No employees found</div>';
            } else {
                let empHtml = '<table class="data-table"><thead><tr><th>ID</th><th>Employee Name</th><th>Basic Salary</th><th>Net Pay</th></tr></thead><tbody>';
                payrollRecords.forEach(rec => {
                    empHtml += `<tr>
                        <td>${rec.id}</td>
                        <td><strong>${rec.employeeName}</strong></td>
                        <td>${rec.basicSalary.toFixed(2)}</td>
                        <td>${rec.netPay.toFixed(2)}</td>
                    </tr>`;
                });
                empHtml += '</tbody></table>';
                employeeContainer.innerHTML = empHtml;
            }

            // Activity Logs
            const logContainer = document.getElementById('activityLogsContainer');
            if (activityLogs.length === 0) {
                logContainer.innerHTML = '<div class="text-secondary" style="padding: 2rem; text-align: center;">No activity logs</div>';
            } else {
                let logHtml = '<table class="data-table"><thead><tr><th>Timestamp</th><th>Action</th><th>Details</th><th>User</th></tr></thead><tbody>';
                activityLogs.slice().reverse().forEach(log => {
                    logHtml += `<tr>
                        <td>${new Date(log.timestamp).toLocaleString()}</td>
                        <td><strong>${log.action}</strong></td>
                        <td>${log.details}</td>
                        <td>${log.user}</td>
                    </tr>`;
                });
                logHtml += '</tbody></table>';
                logContainer.innerHTML = logHtml;
            }

            // Dashboard stats
            document.getElementById('totalEmployees').innerText = payrollRecords.length;
            document.getElementById('totalGross').innerText = `ETB ${totals.grossPay.toFixed(2)}`;
            document.getElementById('totalTax').innerText = `ETB ${totals.incomeTax.toFixed(2)}`;
            document.getElementById('totalNet').innerText = `ETB ${totals.netPay.toFixed(2)}`;
        }

        // Authentication
        function initializeGapiClient() {
            return gapi.client.init({
                apiKey: CONFIG.API_KEY,
                discoveryDocs: ['https://sheets.googleapis.com/$discovery/rest?version=v4'],
            });
        }

        function initializeGisClient() {
            tokenClient = google.accounts.oauth2.initTokenClient({
                client_id: CONFIG.CLIENT_ID,
                scope: CONFIG.SCOPES,
                callback: async (tokenResponse) => {
                    if (tokenResponse.access_token) {
                        gapi.client.setToken({ access_token: tokenResponse.access_token });
                        isAuthenticated = true;
                        document.getElementById('authContainer').classList.add('hidden');
                        document.getElementById('appContainer').classList.remove('hidden');
                        initTabListeners();
                        await loadAllData();
                        switchToTab('dashboard');
                        showAlert('Successfully connected to Google Sheets', 'success');
                    }
                },
            });
        }

        document.getElementById('loginBtn')?.addEventListener('click', () => tokenClient?.requestAccessToken());
        document.getElementById('logoutBtn')?.addEventListener('click', () => {
            isAuthenticated = false;
            gapi.client.setToken(null);
            document.getElementById('authContainer').classList.remove('hidden');
            document.getElementById('appContainer').classList.add('hidden');
        });

        document.getElementById('payrollForm')?.addEventListener('submit', async (e) => {
            e.preventDefault();
            const record = {
                id: document.getElementById('recordId').value,
                employeeName: document.getElementById('empName').value.trim(),
                basicSalary: parseFloat(document.getElementById('basicSalary').value) || 0,
                transportAllowance: parseFloat(document.getElementById('transportAllowance').value) || 0,
                taxableTransport: parseFloat(document.getElementById('taxableTransport').value) || 0,
                overtimePay: parseFloat(document.getElementById('overtimePay').value) || 0,
                loanDeduction: parseFloat(document.getElementById('loanDeduction').value) || 0,
                signature: ''
            };
            await saveRecord(record);
        });

        window.clearForm = clearForm;

        gapi.load('client', async () => {
            await initializeGapiClient();
            initializeGisClient();
        });
    </script>
</body>
</html>
